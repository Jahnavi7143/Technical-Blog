<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Technology Blog</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="./CSS/styles.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <%- include("./partials/header.ejs") %>  

            
        <% if (search_result.length > 0) { %>
            <div class="search-box" >
                <h1 class="search-heading" >Search results for <a href="#" > <%= search_query %> </a></h1>
                <div class="content">
                    <% for(var j=0;j < search_result.length ;j++){ %>
                        <a href= "/post/<%= j+1  %>" >
                            <div class="inner-card">
                                <img src = <%= search_result[j].URL %>  alt="image" class="card-image" />
                                <h1><%= search_result[j].title.length > 26 ? search_result[j].title.substring(0, 27) + "..." : search_result[j].title %></h1>
                                <p><%= search_result[j].Content.length > 296 ? search_result[j].Content.substring(0, 297)  : search_result[j].Content %> <a href="#" style="text-decoration: none;">Read more...</a> </p>
                            </div>
                        </a>
                    <% } %>
                </div>
            </div>
        <% } else { %>
            <div class="not-found content">
                <div class="not-found-2">
                    <h1>400</h1>
                    <p >No results found for <a href="#" > <%= search_query %> </a></p>
                </div>
                
            </div>
        <% } %>

    <!-- Footer -->
    <%- include("./partials/footer.ejs") %>

</body>
</html>